model:
  default: gemini-3.6-flash
  provider: gemini
  api_key: ''

gateway:
  platforms:
    line:
      enabled: true
    telegram:
      enabled: false

memory:
  provider: honcho
  memory_enabled: true

fallback_providers:
  - provider: opencode-free
    model: opencode-free
